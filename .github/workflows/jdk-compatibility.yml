name: JDK compatibility (21/25)

# Keeps Java 17 as the full CI-equivalent/build-release baseline (build.yml + release.yml),
# while adding focused compatibility checks on newer supported JDKs for every PR/push and
# a weekly scheduled run.
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # Every Monday at 07:05 UTC.
    - cron: '5 7 * * 1'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  compatibility:
    name: Focused compatibility (Java ${{ matrix.java }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java: [ '21', '25' ]
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: maven

      - name: Build and test shared engine + both adapters
        # Focused coverage for Java 21/25: compile and run tests for core/engine plus
        # Spring and Quarkus adapter modules, while avoiding browser suites.
        run: >
          ./mvnw -T 1C -B -ntp
          -pl bootui-spring-autoconfigure,bootui-spring-boot-starter,bootui-spring-sample-app,bootui-quarkus,bootui-quarkus-deployment,bootui-quarkus-integration-tests
          -am test

      - name: Exercise Quarkus sample app augmentation
        # Ensures Quarkus build-time augmentation runs on supported JDKs (17/21/25) and
        # catches ByteBuddy/build-plugin regressions without running browser tests.
        run: ./mvnw -T 1C -B -ntp -pl bootui-quarkus-sample-app -am -DskipTests install
