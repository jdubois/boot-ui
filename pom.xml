<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.julien-dubois.bootui</groupId>
    <artifactId>bootui-parent</artifactId>
    <version>1.13.1</version>
    <packaging>pom</packaging>

    <name>BootUI Parent</name>
    <description>BootUI: local-only developer console for Spring Boot 4 and Quarkus applications</description>
    <url>https://github.com/jdubois/boot-ui</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>jdubois</id>
            <name>Julien Dubois</name>
            <url>https://github.com/jdubois</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/jdubois/boot-ui.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/jdubois/boot-ui.git</developerConnection>
        <url>https://github.com/jdubois/boot-ui</url>
        <tag>HEAD</tag>
    </scm>

    <modules>
        <module>bootui-core</module>
        <module>bootui-engine</module>
        <module>bootui-spring-autoconfigure</module>
        <module>bootui-spring-boot-starter</module>
        <module>bootui-spring-boot-starter-reactive</module>
        <module>bootui-ui</module>
        <module>bootui-conformance</module>
        <module>bootui-spring-sample-app</module>
        <module>bootui-spring-webflux-sample-app</module>
        <module>bootui-quarkus-parent</module>
        <module>bootui-quarkus</module>
        <module>bootui-quarkus-deployment</module>
        <module>bootui-quarkus-integration-tests</module>
        <!--
            The Quarkus sample app stays in the always-on reactor so IDEs (e.g. IntelliJ) import it as a
            Java/Maven module on ANY JDK. Its Hibernate ORM build-time augmentation cannot run on a JDK newer
            than the shared Quarkus LTS platform supports, so that work (only) is skipped on JDK 26+ by a profile in
            the module's own pom; plain compile (release 17) + jar still runs everywhere. The module has no
            tests, so nothing fails on an unsupported JDK.
        -->
        <module>bootui-quarkus-sample-app</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <spring-boot.version>4.1.0</spring-boot.version>
        <!-- Latest Quarkus LTS, shared by the extension, integration tests, and sample app. -->
        <quarkus.platform.version>3.33.3.1</quarkus.platform.version>
        <!-- testcontainers version is managed by the Spring Boot BOM (2.0.x) -->
        <!-- opentelemetry-proto remains on -alpha as the upstream project does not publish stable releases yet -->
        <opentelemetry-proto.version>1.10.0-alpha</opentelemetry-proto.version>
        <!-- Flyway 12.x has only verified H2 through 2.3.232; keep migration-backed samples on that version. -->
        <flyway-verified-h2.version>2.3.232</flyway-verified-h2.version>
        <frontend-maven-plugin.version>2.0.2</frontend-maven-plugin.version>
        <central-publishing-maven-plugin.version>0.11.0</central-publishing-maven-plugin.version>
        <cyclonedx-maven-plugin.version>2.9.1</cyclonedx-maven-plugin.version>
        <native-build-tools-plugin.version>1.1.7</native-build-tools-plugin.version>
        <archunit.version>1.5.0</archunit.version>
        <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>
        <central.autoPublish>true</central.autoPublish>
        <maven-antrun-plugin.version>3.2.0</maven-antrun-plugin.version>
        <maven-dependency-plugin.version>3.7.0</maven-dependency-plugin.version>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
        <maven-jar-plugin.version>3.5.1</maven-jar-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <maven-resources-plugin.version>3.5.0</maven-resources-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <palantir-java-format.version>2.94.0</palantir-java-format.version>
        <spotless-maven-plugin.version>3.9.0</spotless-maven-plugin.version>
        <node.version>v24.18.0</node.version>
        <npm.version>11.17.0</npm.version>
        <frontend.test.command>run test</frontend.test.command>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit</artifactId>
                <version>${archunit.version}</version>
            </dependency>
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-engine</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-spring-autoconfigure</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-spring-boot-starter</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-spring-boot-starter-reactive</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-ui</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-conformance</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-quarkus</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.julien-dubois.bootui</groupId>
                <artifactId>bootui-quarkus-deployment</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.15.0</version>
                    <configuration>
                        <release>${java.version}</release>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven-dependency-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>mockito-agent-property</id>
                            <phase>initialize</phase>
                            <goals>
                                <goal>properties</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.6</version>
                    <configuration>
                        <!-- A modest, fixed number of test-class forks, reused across classes assigned to
                             each one (JVM start-up is amortized instead of paid per class). This is
                             process-level parallelism only — different forks are separate JVMs, so it
                             needs no in-JVM thread-safety from test code, and every test in this reactor
                             that binds a real socket already binds to port 0 (ephemeral) rather than a
                             fixed port (embedded HttpServer fixtures in bootui-engine/
                             bootui-spring-autoconfigure/bootui-quarkus, @SpringBootTest's
                             WebEnvironment.RANDOM_PORT everywhere, and quarkus.http.test-port=0 in every
                             bootui-quarkus-integration-tests/* submodule); the only fixed-port literals
                             elsewhere are inert (ArchUnit bytecode-scan fixtures that are never actually
                             instantiated, or plain ints/strings in mocks and DTOs).

                             Deliberately NOT scaled to core count (e.g. 1C): CI (.github/workflows/build.yml)
                             and CONTRIBUTING.md's recommended local command both build with -T 1C, so
                             several sibling modules' own Surefire runs can already be executing concurrently
                             (e.g. bootui-spring-autoconfigure and bootui-quarkus depend only on
                             bootui-engine, so they are reactor-parallel with each other). forkCount scaling
                             to the core count in each of those concurrent executions compounds with
                             reactor-level parallelism and oversubscribes CPU enough to trip real timeouts
                             (observed: a WebTestClient read exceeding its default 5s budget under that
                             combined load). A small fixed count still gives every large test module
                             (bootui-engine, bootui-spring-autoconfigure) real intra-module parallelism
                             without multiplying out of control. -->
                        <forkCount>2</forkCount>
                        <reuseForks>true</reuseForks>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                    <configuration>
                        <propertiesEncoding>${project.build.sourceEncoding}</propertiesEncoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.github.eirslett</groupId>
                    <artifactId>frontend-maven-plugin</artifactId>
                    <version>${frontend-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>${maven-antrun-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <configuration>
                        <forceCreation>true</forceCreation>
                        <includePom>true</includePom>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <configuration>
                        <doclint>none</doclint>
                        <failOnError>false</failOnError>
                        <quiet>true</quiet>
                        <!--
                            Trim static doclet boilerplate that scales with class count instead of actual
                            documentation content, to help stay under Maven Central's new monthly publishing
                            quota (see https://central.sonatype.org/publish/maven-central-publishing-limits/):
                            noindex/notree/nohelp drop the master index, class-hierarchy tree, and per-page
                            help link. No loss of per-class Javadoc content, just the generated index chrome.
                        -->
                        <noindex>true</noindex>
                        <notree>true</notree>
                        <nohelp>true</nohelp>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${maven-gpg-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.cyclonedx</groupId>
                    <artifactId>cyclonedx-maven-plugin</artifactId>
                    <version>${cyclonedx-maven-plugin.version}</version>
                    <!--
                        Deliberately not bound to any lifecycle phase: cyclonedx-maven-plugin attaches its
                        BOM as an extra artifact with a "cyclonedx" classifier during install/deploy, which
                        would otherwise get swept into Maven Central publication by the release profile and
                        count against Central's per-file monthly quota (see the checksums comment above) for
                        every published module. CI instead invokes the makeAggregateBom goal explicitly
                        (.github/workflows/build.yml) after the reactor build, producing one SBOM covering the
                        whole build without touching release artifacts.
                    -->
                    <configuration>
                        <projectType>application</projectType>
                        <schemaVersion>1.6</schemaVersion>
                        <outputFormat>json</outputFormat>
                        <outputName>bootui-sbom</outputName>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.sonatype.central</groupId>
                    <artifactId>central-publishing-maven-plugin</artifactId>
                    <version>${central-publishing-maven-plugin.version}</version>
                    <extensions>true</extensions>
                    <configuration>
                        <publishingServerId>central</publishingServerId>
                        <autoPublish>${central.autoPublish}</autoPublish>
                        <!--
                            This extension stages the whole reactor from its root execution. Per-module
                            maven.deploy.skip values are therefore not sufficient to keep non-distribution
                            modules out of the Central bundle. Exclude them explicitly with the plugin's
                            supported reactor-level filter.
                        -->
                        <excludeArtifacts>
                            <excludeArtifact>bootui-conformance</excludeArtifact>
                            <excludeArtifact>bootui-coverage</excludeArtifact>
                            <excludeArtifact>bootui-spring-sample-app</excludeArtifact>
                            <excludeArtifact>bootui-spring-webflux-sample-app</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-sample-app</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-integration-tests-aggregator</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-cache-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-datasource-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-email-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-flyway-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-health-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-hibernate-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-liquibase-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-micrometer-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-otel-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-prod-shell-guard-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-rabbit-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-rest-client-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-scheduler-integration-tests</excludeArtifact>
                            <excludeArtifact>bootui-quarkus-security-integration-tests</excludeArtifact>
                        </excludeArtifacts>
                        <!--
                            Sonatype only mandates md5+sha1 checksums (sha256/sha512 are optional, see
                            https://central.sonatype.org/publish/requirements/#provide-file-checksums). The
                            plugin's default ("all") generates all four, roughly doubling the checksum/signature
                            file fanout per artifact and eating into Central's new monthly file-count quota
                            (see https://central.sonatype.org/publish/maven-central-publishing-limits/) for no
                            validation benefit. "required" drops sha256/sha512 while staying fully compliant.
                        -->
                        <checksums>required</checksums>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <configuration>
                    <java>
                        <palantirJavaFormat>
                            <version>${palantir-java-format.version}</version>
                        </palantirJavaFormat>
                        <removeUnusedImports/>
                    </java>
                    <formats>
                        <format>
                            <includes>
                                <include>*.md</include>
                                <include>.github/**/*.md</include>
                                <include>.github/**/*.yml</include>
                                <include>docs/**/*.md</include>
                                <include>pom.xml</include>
                                <include>src/**/*.json</include>
                                <include>src/**/*.properties</include>
                                <include>src/**/*.xml</include>
                                <include>src/**/*.yaml</include>
                                <include>src/**/*.yml</include>
                            </includes>
                            <excludes>
                                <exclude>**/dist/**</exclude>
                                <exclude>**/node_modules/**</exclude>
                                <exclude>**/playwright-report/**</exclude>
                                <exclude>**/target/**</exclude>
                                <exclude>**/test-results/**</exclude>
                            </excludes>
                            <trimTrailingWhitespace/>
                            <endWithNewline/>
                        </format>
                    </formats>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>coverage</id>
            <modules>
                <module>bootui-coverage</module>
            </modules>
            <properties>
                <frontend.test.command>run test:coverage</frontend.test.command>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>prepare-coverage-agent</id>
                                <phase>initialize</phase>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                                <configuration>
                                    <!-- Keep the existing Surefire fork topology while isolating each fork's
                                         execution data. The merge below produces the standard jacoco.exec consumed
                                         by module checks/reports and the aggregate report. -->
                                    <destFile>${project.build.directory}/jacoco-${surefire.forkNumber}.exec</destFile>
                                    <append>true</append>
                                </configuration>
                            </execution>
                            <execution>
                                <id>merge-coverage-forks</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>merge</goal>
                                </goals>
                                <configuration>
                                    <fileSets>
                                        <fileSet>
                                            <directory>${project.build.directory}</directory>
                                            <includes>
                                                <include>jacoco-*.exec</include>
                                            </includes>
                                        </fileSet>
                                    </fileSets>
                                    <destFile>${project.build.directory}/jacoco.exec</destFile>
                                </configuration>
                            </execution>
                            <execution>
                                <id>module-coverage-report</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>critical-coverage-check</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                                <configuration>
                                    <rules>
                                        <rule>
                                            <element>CLASS</element>
                                            <includes>
                                                <include>io.github.jdubois.bootui.core.SecretMasker</include>
                                            </includes>
                                            <limits>
                                                <limit>
                                                    <counter>LINE</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.70</minimum>
                                                </limit>
                                            </limits>
                                        </rule>
                                        <rule>
                                            <element>CLASS</element>
                                            <includes>
                                                <include>io.github.jdubois.bootui.core.BootUiPathNormalizer</include>
                                            </includes>
                                            <limits>
                                                <limit>
                                                    <counter>LINE</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>1.00</minimum>
                                                </limit>
                                                <limit>
                                                    <counter>BRANCH</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>1.00</minimum>
                                                </limit>
                                            </limits>
                                        </rule>
                                        <rule>
                                            <element>PACKAGE</element>
                                            <includes>
                                                <include>io.github.jdubois.bootui.engine.safety</include>
                                            </includes>
                                            <limits>
                                                <limit>
                                                    <counter>LINE</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.90</minimum>
                                                </limit>
                                                <limit>
                                                    <counter>BRANCH</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.75</minimum>
                                                </limit>
                                            </limits>
                                        </rule>
                                        <rule>
                                            <element>CLASS</element>
                                            <includes>
                                                <include>io.github.jdubois.bootui.autoconfigure.config.BootUiExposure</include>
                                                <include>io.github.jdubois.bootui.autoconfigure.mcp.SpringMcpPanelPolicy</include>
                                                <include>io.github.jdubois.bootui.quarkus.QuarkusExposurePolicy</include>
                                                <include>io.github.jdubois.bootui.quarkus.mcp.QuarkusMcpPanelPolicy</include>
                                            </includes>
                                            <limits>
                                                <limit>
                                                    <counter>LINE</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.80</minimum>
                                                </limit>
                                                <limit>
                                                    <counter>BRANCH</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.60</minimum>
                                                </limit>
                                            </limits>
                                        </rule>
                                    </rules>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!--
            The Hibernate Quarkus integration-test module depends on Hibernate ORM, whose build-time ByteBuddy
            enhancement (via the selected Quarkus platform) cannot read class files newer than the JDKs that platform
            supports (17, 21 and 25). It is a @QuarkusTest module whose tests force that augmentation, so it cannot
            simply skip the work the way the sample app can; it is therefore wired into the reactor only on a
            supported JDK so that:
              - CI and releases (JDK 17) build and augment it, exercising the full Hibernate-advisor light-up; while
              - a local `./mvnw install` on a newer, unsupported JDK (e.g. 26) stays green by skipping it.
            Build it explicitly on a supported JDK, e.g. with JAVA_HOME pointed at a JDK 17/21/25:
              JAVA_HOME=/path/to/jdk-17 ./mvnw -pl bootui-quarkus-integration-tests/hibernate -am install
            The Quarkus sample app is NOT gated here: it lives in the always-on <modules> list above (so IDEs
            import it on any JDK) and skips only its Hibernate augmentation on JDK 26+ via a profile in its own pom.
            The extension's own modules (bootui-quarkus*, the non-Hibernate integration-tests) have no Hibernate
            dependency and stay in the always-on reactor above, so they are built and tested on every JDK.
        -->
        <profile>
            <id>quarkus-sample-app</id>
            <activation>
                <jdk>[17,26)</jdk>
            </activation>
            <modules>
                <module>bootui-quarkus-integration-tests/hibernate</module>
            </modules>
        </profile>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <configuration>
                            <gpgArguments>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                        </configuration>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
