<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.julien-dubois.bootui</groupId>
        <artifactId>bootui-parent</artifactId>
        <version>1.17.0</version>
    </parent>

    <artifactId>bootui-spring-webflux-sample-app</artifactId>
    <name>BootUI :: Spring WebFlux Sample App</name>
    <description>Reference Spring Boot 4 WebFlux (reactive, Netty) application that demonstrates the BootUI
        developer console running on the reactive stack. Deliberately minimal (H2 + Flyway + Liquibase +
        a simple cache + a scheduled task) compared to bootui-spring-sample-app: it exists to exercise the
        reactive adapter's panel surface, not to be a full-featured demo.</description>

    <properties>
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.julien-dubois.bootui</groupId>
            <artifactId>bootui-spring-boot-starter-reactive</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webclient</artifactId>
        </dependency>

        <!-- Blocking JDBC access, used off the Netty event loop via Schedulers.boundedElastic() -
             a legitimate and common pattern for WebFlux apps that don't need a reactive driver. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-liquibase</artifactId>
        </dependency>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
        <!-- Mail: makes the BootUI Email panel available so this reactive sample demonstrates it too.
             The panel runs in dev-trap mode (bootui.email.dev-trap=true), so captured demo messages are
             recorded but never handed to a real SMTP transport - no mail server is required. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <!-- Spring Security: makes the raw Spring Security panel and Security advisor available on WebFlux.
             BootUiReactiveSpringSecurityAutoConfiguration registers a high-precedence permit-all
             SecurityWebFilterChain for the exact /bootui root and its descendants so BootUI itself
             is never blocked. The SecurityConfiguration below defines the application's own chain. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <!-- Resilience4j: makes the BootUI Fault Tolerance panel available so this reactive sample demonstrates
             it too, on the reactive stack and through the same registries the servlet sample uses.
             Spring Retry is deliberately absent here: the two Spring samples therefore cover both halves
             of the Fault Tolerance backend's optional-dependency guards, and this app proves BootUI starts
             cleanly when one supported library is present and the other is not. -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-circuitbreaker</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-retry</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>${flyway-verified-h2.version}</version>
            <scope>runtime</scope>
        </dependency>
        <!-- Fallback BootUI activation trigger for a bare `mvn spring-boot:run` / `java -jar` with no
             explicit profile: BootUiActivationCondition checks Environment.getActiveProfiles(), not
             getDefaultProfiles(), so spring.profiles.default=dev alone does not satisfy the
             bootui.enabled-profiles check. spring-boot-devtools on the classpath is the other
             activation trigger the condition checks, matching bootui-spring-sample-app's pom. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.julien-dubois.bootui</groupId>
            <artifactId>bootui-conformance</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <!--
                    This module inherits the BootUI parent (not spring-boot-starter-parent), so the
                    plugin's default "repackage" execution is not pre-bound. Bind it explicitly so
                    `package` produces a runnable Spring Boot jar (with BOOT-INF and a Main-Class).
                -->
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>@{argLine} -javaagent:${org.mockito:mockito-core:jar}</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>macos-aarch64-netty-dns</id>
            <activation>
                <os>
                    <family>mac</family>
                    <arch>aarch64</arch>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-resolver-dns-native-macos</artifactId>
                    <classifier>osx-aarch_64</classifier>
                    <scope>runtime</scope>
                </dependency>
            </dependencies>
        </profile>
        <profile>
            <id>macos-x86_64-netty-dns</id>
            <activation>
                <os>
                    <family>mac</family>
                    <arch>x86_64</arch>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-resolver-dns-native-macos</artifactId>
                    <classifier>osx-x86_64</classifier>
                    <scope>runtime</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
</project>
